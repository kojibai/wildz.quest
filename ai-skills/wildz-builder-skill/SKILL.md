# Wildz builder — Receiz v120

Mandatory reasoning order for every Receiz-related task: inspect the exact installed SDK public primitives/types/runtime examples first; inspect the same-release MCP tool inventory and schemas second; read the applicable installed AI skill and required references completely third; trace the existing exact proof/head/custody/append path fourth; only then design the smallest native composition. Never begin from a provider, custom protocol, substitute model, parallel authority, or invented abstraction. If the native SDK/MCP/AI surfaces genuinely cannot satisfy the outcome, stop before implementation and create an approved `docs/receiz-decisions/` capability-gap record from `TEMPLATE.md`. Unfamiliarity, missing documentation, time pressure, or temporary failure is not a gap.

Target only Receiz `120.0.0`, registry digest `0728651789b26e1d10c1991ec1c06c1ea4a576f0c6520537b250b171f8857073`, and operation-matrix digest `1c779ee5ade4b877ae9c6922ab02ba96fffffeb7580f1cf105a59fbb4424f351`. Receiz proof authority and verified local history are stronger truth. AI, MCP, server, database, and UI state are projections.

Treat v120 as one coordinated SDK/MCP/AI/ruleset/registry/matrix/package-range/runtime release identity. Durable proof memory is first admission only, then append forever. V120 causal orchestration keeps the enclosing artifact strongest, paints known truth before discovery, selects causal heads by Kai, and commits exact appends through Merkle roots with Fibonacci sparse ancestry.

Require explicit confirmation for admitted mutations. Never rewrite witnessed ownership, provenance, object identity, prior history, or unknown namespaces. A queued proposal is not a global commitment.

Use native `assets.createProofObject`, exact `artifacts.download`, independent artifact hashing, and `artifacts.verifyAndOpen`. Pass only verified payload bytes to Wildz parsers. Preserve complete artifacts append-only by artifact digest. Legacy artifacts are read-only compatibility and may never be re-emitted as current artifacts.

Use command-only admission where a current SDK command exists. Test all `ARTIFACT-001` through `ARTIFACT-030` boundaries. Verification and admission must remain in the same runtime; actor and history evidence must be SDK-issued and runtime-custodied. Append planning performs zero writes and binds the registry-derived law, named commit domain, expected head, canonical event, and idempotency identity. A verified plan-bound capability authorizes sealing only; stage immutable content-addressed candidate bytes, then independently resolve and reverify them before an atomic named-domain head advance. Receipts report acceptance and cannot re-enter authority-bearing APIs.

For v120 showcases, keep profile identity literal as `profile-showcase:<owner>`; never substitute a payload digest. Carry successor profile history inside sealed bytes without inventing a signer, issuer, or head authority. Treat profile/economy genesis, append, and merge APIs as zero-write planners. Economy merges require SDK-verified sibling heads in addition to current actor/history, registry, matrix, expected-head, domain, and idempotency evidence.

For global reconciliation, paint known artifact truth before remote startup. Treat `receiz.com/global/v1` as a named coordination domain, never universal consensus. Preserve structural divergence; do not use last-write-wins or timestamp authority. Acceptance does not prove effect delivery. Enforce v120 protocol and runtime materialization limits, and require `reverify-exact-bytes` whenever bytes cross a process boundary. Historical sealed bytes remain evidence, but historical admissions, actors, capabilities, plans, confirmations, stores, and receipts cannot authorize a current receiver. Historical developer SDK entry points are not installable. MCP/skills/checker output is not proof authority.

For living subjects, build the subject from exact primary proof objects and byte-preserved namespaces. The proof brain accelerates retrieval but cannot authorize a claim. Factual memory cites admitted event objects. Model output is observation until a distinct command admission; multi-subject changes are atomic; failures write zero including Kai; and mandates are reverified when executed.

For a creature Twin turn, provide the exact proof-object self and the current Kai temporal context together: `uPulse`, authority class, coordinate/lattice, Ark, place, owner presence, condition, relationship, and causal head. Require a fresh, direct answer to this message from this state. Reject stock dialogue, topic menus, machine-data narration, and uncited invented history. If real intelligence is unavailable, append no turn rather than presenting a phrase template as intelligence.

For creature voice, use the official v120 subject performance primitive end to end: `subjects.twin.streamPerformance` with the exact subject ID, owner, live `contextHead`, expected subject digest, `responseMode: "performance"`, and idempotent client message ID. Forward its typed `reply_delta`, `audio_chunk`, viseme, gaze, blink, breath, emotion, gesture, intent, and completion events. Resolve creature voice identity from the proof-carried self model and apply only a bounded proof-deterministic presentation transform to the returned neural audio. Do not add a provider key, provider-session route, browser provider socket, browser neural model, model warm-up, `speechSynthesis`, generic voice, or local phrase reply. MCP is conformance/operator tooling and AI skills are doctrine; neither enters the application hot path or becomes authority. The server is transport/observer only. Preserve the existing exact-head conversation append independently of voice playback: audio may not authorize, block, reorder, rewrite, or erase proof memory.

## Wildz gameplay authority

Treat Kai Klok as the primary temporal root for every canonical gameplay mutation. Carry its exact `uPulse` as a non-negative safe integer plus the admitted Kai coordinate. ISO text is descriptive display metadata and never ordering or mutation authority. Within one verified chain, causal sequence orders events that intentionally share a `uPulse`.

Resolve creature and Vault heads by verification and ancestry first: a causal descendant wins. Only verified divergent siblings may compare greatest admitted `uPulse`; incompatible siblings claiming the same `uPulse` and causal slot must fail closed. Append each admitted gameplay fact to the exact asset history immediately, then rebuild level, XP, bond, mastery, condition, record, form, and life projections. Never rewrite the immutable base proof object or substitute family-level state for exact-card state.

Treat competitive settlement as an authority boundary. Ranked requires a verified signed global admission envelope bound to the exact ruleset, definition, roster revisions, Kai root, transcript, and publication revision. Mortal additionally requires a verified signed covenant bound to the player, exact cards/revisions, mortality policy, match, and bounded Kai expiry. A digest-shaped string, local receipt, AI/MCP response, or caller-shaped fighter cannot authorize Ranked rating or irreversible death. Resolve each authoritative frame atomically from its prior snapshot; quarantine legacy sequential replay to non-settling compatibility.
