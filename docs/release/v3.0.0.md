# Wildz v3.0.0

Wildz v3 is the standalone world, living-card collection, identity, social, and market PWA release candidate. The package is versioned `3.0.0`, and the exact local candidate has passed its repository release gate.

## Receiz v113 toolchain

- `@receiz/sdk 113.0.0` is pinned exactly for application identity, artifact, native proof-object, constitutional compiler, checker, and rail integration.
- `@receiz/mcp-server 113.0.0` is pinned exactly for operator tooling.
- `@receiz/ai-skills 113.0.0` is pinned exactly for v113 operator procedures.

The finalized v113 SDK, MCP, and AI-skills packages resolve directly from the official npm registry, and their published integrity values are lockfile-pinned. Wildz does not fork, patch, or replace SDK proof logic. The release and ruleset are locked to registry digest `4c4aa85f9785d205dcf7e4e5109837a83f8c3bf8e166130ae7e87353f299c637` and operation-matrix digest `091ab9e6b3acb05283510a19754e53c637dbd96b47b499a524dc44c34f8e783b`.

MCP and AI skills do not outrank SDK verification, proof-object continuity, ownership heads, settlement admission, or player confirmation. Their installation does not prove that they were invoked during release qualification.

`receiz.app.json` declares the v113 Wildz contract with `identity`, `proof`, `proofMemory`, `publicStore`, and `commerce`, artifact-first authority, and database authority disabled. The official compiler produces the deterministic plan represented by `receiz.generated.json`. `pnpm receiz:check` runs the v113 repository checker and participates in `pnpm release:check`; it validates repository evidence and the exact release, ruleset, registry, operation matrix, limits, and 30 artifact laws. Canonical mutation is command-only and requires same-runtime verified admission/history/actor evidence, a plan-bound capability, named commit domain, immutable staging, independent byte resolution, expected head, idempotency, and bounded registry-derived effects. Compiler/checker success is not proof authority, strict-live evidence, or remote-mutation evidence.

The historical forward-only checkpoint remains compatibility evidence and rewrote no sealed history. Current execution is v113-only; historical runtime authority objects cannot authorize a current receiver, and a queued proposal is not a global commitment.

## V3 highlights

- Owner-scoped continuity covers the player’s cards, support selection, settings, history, receipts, and canonical world cursor.
- Identity Seal/key flows activate the canonical Receiz account through the official v113 signed continuation. A verified legacy player Vault restores its embedded Wildz player and complete collection into an artifact-scoped Wildz session; without owner-continuity proof it cannot invent canonical account authority.
- Historical-owner cards from a verified cross-platform Vault remain playable through a compact Merkle custody proof rooted only after exact server verification. Duplicate leaves collapse, lineage conflicts fail, and gameplay never re-sends the complete collection.
- The world model includes ecology, settlements, social memory, raids, bosses, mastery, routes, and canonical event projections.
- Public profiles and public cards use sanitized canonical routes and durable Receiz projection contracts.
- Listings, offers, trades, checkout, and transfers require current ownership, expected revision, idempotency, actor authority, and verified Receiz admission.
- The v113 SDK does not expose the Wildz-specific conditional market ownership append; those mutations fail closed as unavailable until that verified capability exists.
- Checkout creates no synthetic ownership: a settled result requires a Receiz Connect transfer proof, the matching wallet-ledger event, and a conditional market ownership append; retries reuse the same transfer key.
- New exports use the v113 native Record → Seal flow and download the SDK-returned artifact byte-for-byte. The `wildz-v113` idempotency namespace binds retries to the payload digest. Existing legacy proof objects remain read-only compatible only through v113 `verifyAndOpen`.
- Unified admission and recovery planning are zero-write SDK projections. Recovery commits require verified capability and atomic storage; neither a plan nor an explanation is proof authority.
- V113 bearer ownership receives a complete SDK-opened artifact handle, derives prior ownership from carried proof, and returns a new native artifact that Wildz downloads and reopens. Global reconciliation uses the named `receiz.com/global/v1` domain, preserves structural divergence, and reports acceptance separately from effect delivery.
- The installable PWA uses release-distinct caches, explicit install/update consent, a state-preserving update transition, and security response headers.

## Identity and proof authority

Receiz proof and Identity Seal verification are authoritative. Complete artifacts cross v113 `verifyAndOpen` before payload parsing. New proof creation accepts only native continuity whose exact bytes, artifact/payload digests, Signature V4, owner, record, claim, verify path, and final reopen agree. Legacy objects are admitted only as `verified-legacy-read`. Stale, conflicting, malformed, resource-exhausting, or unverifiable material fails closed.

An identity-bound or v113 owner-continuity Vault can establish canonical current identity and custody. A legacy proof-sealed player Vault establishes exact artifact recovery authority only: its scoped principal is derived from the verified artifact, and the carried handle is presentation data until Identity Seal/key authority is proven. Historical owner coordinates inside cards remain immutable provenance; they do not prevent a compatible cross-platform Vault from restoring the complete collection. Exact duplicate cards are dropped, a strictly verified newer revision wins, and divergent same-ID proof forks fail.

Local IndexedDB preserves owner-scoped state plus an append-only complete-artifact history keyed by artifact digest. It never replaces sealed proof, publication, market, or settlement authority, and an existing digest cannot be overwritten with different bytes or provenance.

## Exact offline boundary

Previously visited public profiles, previously visited public cards, and cached card details may remain readable. The versioned app shell can start without a network connection and owner-scoped verified local state may still be present on the device.

Sign-in, the live world, social presence, the market, publishing, listing, trade, transfer, and payment require a connection. Authentication, world, market, Receiz, artifact-proxy, and mutation traffic is not cached. An unvisited public page receives the offline guidance page rather than another player’s or the root document. This boundary does not claim that the live game or mutations work offline.

## Remote capability boundary

There is no external database added by this release. Durable shared world, profile, social, and economy state is admitted through configured Receiz rails. The authenticated world bootstrap requires a server-only Receiz Connect app/service credential; it is never a generated player token. Durable Receiz rails fail closed when unavailable, stale, or unverifiable; no fallback reports a remote mutation as successful. A checkout session is not settlement, and ownership does not transfer before settlement admission.

## Qualification status

The exact local candidate passed the v113 application-contract and release-identity checker, complete Node test suite, typecheck, lint, full tracked/untracked text secret scan, production build, MCP conformance, and default Receiz doctor. Prior browser and artifact exercises remain historical evidence and must not be misreported as a fresh v113 run. Because the legacy fixture contains no Identity Seal or v113 owner-continuity binding, the release treats it as artifact-scoped recovery rather than canonical global account authentication.

The strict-live doctor was attempted and failed closed before live probes because production configuration was absent. Authorized strict-live verification, remote mutation, the external six-writer exercise, deployment, tag, push, and production publication remain pending. The local candidate is ready to commit; production activation remains conditional on those external gates.
