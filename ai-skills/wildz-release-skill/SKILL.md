# Wildz v3 Release

Receiz proof authority and verified, sanitized evidence define release truth. Target Wildz `3.0.0` with exact `@receiz/sdk@106.0.0`, `@receiz/mcp-server@106.0.0`, and `@receiz/ai-skills@106.0.0`, resolved together at major 106 and registry digest `bf851c209e807309672c0f466411baa5607ce6b3195fe4eb16755edfeb7f5a1a`.

## Procedure

1. Start with read-only diagnostics: inspect the exact diff, requested and installed versions, official npm registry and lockfile resolution, idle processes, and required environment names without printing values.
2. Compile and check `receiz.app.json` at target `106.0.0`. Require artifact-first authority, database authority disabled, authenticated Record before Seal, durable proof memory, continuity verification, idempotency, command-only mutation, and browser-safe server secrets.
3. Verify the exact v106 release identity and registry digest. Use plan/permit/execute with exact permit-digest confirmation for admitted operations, then run the repository checker, law coverage, mutation tests, replay equivalence, MCP conformance, independent verifier, tests, typecheck, lint, secret scan, build, and default doctor.
4. Run strict-live only with an authorized environment. A configured value is not capability proof; record actual sanitized probe status and stop on failure.
5. Validate every external artifact writer from sanitized provenance and recomputed digests. Do not infer compatibility from a filename, fixture, or package installation.
6. Exercise WebKit and Chromium, required dimensions, direct safe API reads, accessibility, renderer recovery, offline/install/update behavior, configured and unavailable market modes, and current-Wildz export readers. V106 has no Wildz-specific conditional market append surface, so require the live market path to fail closed when capability is absent.
7. Record exact commands and results. Mark pending or failed evidence honestly; never convert missing evidence into a pass.

MCP, AI skills, compiler output, registry evaluation, and release locks never outrank SDK proof verification. Deployment, tagging, pushing, publication, credential changes, private-artifact access, release creation, or any live mutation requires explicit human confirmation. Never print secrets or private evidence.
