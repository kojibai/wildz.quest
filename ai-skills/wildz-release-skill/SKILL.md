# Wildz Release

Receiz proof authority and verified artifacts define release truth. Run tests, typecheck, lint, build, secret scan, real mobile browser playthrough, PWA/offline checks, and payment-failure checks. Deployment, publication, credential changes, and production mutation require human confirmation. Record evidence and never print secrets.
