# Wildz release — Receiz v109

Release only with exact `@receiz/sdk@109.0.0`, `@receiz/mcp-server@109.0.0`, and `@receiz/ai-skills@109.0.0` from the official npm registry, ruleset `109.0.0`, and registry digest `17f76b37c9fcd46f710239b5c1660b03cc34ec64bed30d1cc45c18d5d40eab70`.

Receiz proof authority and verified local history are stronger truth. Require explicit confirmation for publication or deployment. A queued proposal is not a global commitment. MCP, AI, compiler, checker, registry evaluation, and release locks remain below sealed proof authority.

Release evidence must cover `ARTIFACT-001` through `ARTIFACT-011`: native carrier, Signature V4, exact artifact digest, payload binding, owner/claim/path, independent zero-network local verification, exact-byte round trip, cross-application continuity, append-only history, bearer ownership, and verified legacy-read compatibility. Historical developer SDK entry points must remain unavailable. Run command-only admission tests, mutation tests, replay equivalence, MCP conformance, independent verifier, typecheck, lint, secret scan, build, doctor, and the release lock. Refuse completion on any missing or failing evidence.
