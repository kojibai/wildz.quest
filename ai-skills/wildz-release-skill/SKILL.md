# Wildz release — Receiz v119

Release only with exact public npm packages `@receiz/sdk@119.0.0`, `@receiz/mcp-server@119.0.0`, and `@receiz/ai-skills@119.0.0`, their published SHA-512 values verified in `pnpm-lock.yaml`, ruleset `119.0.0`, registry digest `49c167a437ec7c0e486412dd62c54af4abdf94eda1ebc18d263a027d105cecd9`, and operation-matrix digest `53cf9d6862b2396e2fe7864f8607c00c4e3b6e31b082ab5c5c8dff088fcb52c1`.

Qualify v119 as one coordinated SDK/MCP/AI/ruleset/registry/matrix/package-range/runtime release identity. Durable proof memory is first admission only, then append forever. V119 causal orchestration keeps the enclosing artifact strongest, paints known truth before discovery, selects causal heads by Kai, and commits exact appends through Merkle roots with Fibonacci sparse ancestry.

Receiz proof authority and verified local history are stronger truth. Require explicit confirmation for publication or deployment. A queued proposal is not a global commitment. MCP, AI, compiler, checker, registry evaluation, and release locks remain below sealed proof authority.

## Competitive-alpha evidence procedure

Treat Kai Klok as the primary temporal root. Verify every canonical coordinate carries exact `uPulse` as a non-negative safe integer; ISO is descriptive display metadata and never ordering authority. Prove causal descendant ordering, same-`uPulse` sequence ordering, greatest admitted `uPulse` resolution for verified divergent siblings, and fail-closed same-slot conflicts.

Verify creature history from the SDK-verified enclosing card or Vault. Check exact asset identity, causal parent, idempotency, admitted Kai coordinate, bounded effects, event digest, and resulting projection digest. Rebuild level, XP, bond, mastery, growth, condition, record, form, and life state; prove the base proof object stays byte-identical across append, card export/import, and Vault export/import. Reject mutation, fork, resurrection, or family-cache substitution.

Audit each Arena replay against the exact definition and ruleset, pinned fighter revisions, Kai root, atomic frame transcript, checkpoints, terminal digest, and verified admission envelope. Ranked requires verified global admission and cannot retire a creature. Mortal requires a verified player-bound, match-scoped, revision-pinned covenant whose Kai expiry remains valid at settlement. Record admission, replay, publication, rating, consequence append, and effect delivery as distinct outcomes.

Review tournament health and season integrity using admitted results only: ruleset/version spread, replay rejection, disconnect and forfeit rates, matchup and rating distribution, settlement latency, publication backlog, covenant failures, and unresolved divergence. Label sample size and missing telemetry. Never infer a healthy tournament or complete season from local tests alone.

Balance analysis may run deterministic simulations and counterfactual replay. Coaching may produce opt-in recommendations from public or player-authorized evidence, but coaching and simulation are not gameplay, matchmaking, rating, settlement, or proof authority. Never expose hidden opponent input or let an agent choose live-player actions. AI and agents cannot sign or admit an envelope, covenant, result, creature event, publication, or release.

Use MCP for read-only audit and capability inspection first. Invoke only installed v119 tools for documented operations; do not invent product-specific tools or claim a live capability from documentation. Any tournament command, publication, settlement, deployment, or release requires explicit confirmation of the exact reviewed command/permit digest immediately before execution. Preserve plan, attempt, admission, and delivery evidence separately.

Release evidence must cover `ARTIFACT-001` through `ARTIFACT-030`: native carrier, Signature V4, exact artifact and payload digests, owner/claim/path binding, independent zero-network local verification, cross-application continuity, append-only history, bearer ownership, verified legacy reads, runtime-custodied admission/history/actor evidence, zero-write planning, registry-derived effects, named atomic commit domain, plan-bound capability, immutable staging, independent staged-byte resolution, report-only receipts, bounded verification/materialization, known-artifact-first paint, structural-only divergence handling, and distinct plan/attempt/commit confirmations. It must also prove all 16 v119 operations, literal `profile-showcase:<owner>` identity, sealed carried successor history, no new profile signer/issuer/head authority, and verified sibling heads for economy merges. Historical developer SDK entry points must remain unavailable. The MCP inventory must be exactly the nine current v119 artifact tools, including global resolve and offline reconcile plan/stage/commit. Run command-only admission tests, mutation tests, replay equivalence, MCP conformance, independent verifier, typecheck, lint, secret scan, build, doctor, and the release lock. Refuse completion on any missing or failing evidence.
