# Independent crew roster integration

Package baseline: SDK, MCP and AI skills 126.0.0. The installed v126 mandate SDK/MCP maps retain V122-named operations; these are the prescribed exports, not a package downgrade. `pnpm receiz:check` passes.

All owned living creatures can now be dispatched from the crew panel. Selecting a different active or support companion preserves other trips and their actual positions. Guards index cards once per immutable inventory revision; changing one proof invalidates only that creature. Restoration reads three records per yielded batch. The journal services three runtime entries per 500 ms tick with fair rotation. Other-space trips preserve positions and resume on returning to their loaded space if the original proof remains valid.

Independent physical movement uses the existing custom swept collision sampler. Two entries receive a 100 ms step per timer tick, with at most one 192-node path plan and twelve cached routes. Evicted detours still make progress; an unreachable actor cannot monopolize planning. No elapsed-time catch-up or predicted arrival is performed. Larger rosters share that work budget, so travel speed is not claimed constant as roster size increases. Only the closest two/three/four independent models render at low/medium/high quality. Bodies and labels share interpolated actual movement segments; rendering never changes authoritative positions. Missing geometry outside the loaded neighborhood pauses work.

The material-worker coordinator prepares the existing `resource.material.harvest` / `commons.harvest` command, including real physical arrival, exact source head, source mandate and distinct SDK subject/proof bindings. Its production hook port retains the exact Kai-rooted command. The journal commits its pending job-head fence after asynchronous source preparation and reauthorization, immediately before persistence. Recall before that fence prevents material admission; uncertain completion uses exact lookup rather than another harvest. Tests exercise the real local WorldService and source queue, including the admitted material lot. The current source law assigns that lot to its owner immediately; this is not a worker-custody or delivery implementation.

Full autonomous materials remain gated: the canonical proof/source-card/mandate authorization port must be supplied and verified before enabling assignments. No successful live mandate issuance, separate hauling custody, or autonomous construction has been claimed. The subsequently approved disposable upload and successful exact sealed restore are recorded in crew-proof-source-integration.md. Current continuous roaming and material-work scope are recorded in crew-continuous-follow-and-material-work.md.

Reference ledger: threejs-gameplay-systems gameplay-workflows.md and physics-engine-selection.md read; existing custom collision retained. Receiz v126 autonomous-mandate SKILL.md, manifest, SDK map, MCP map, examples and test contract read. No sibling Receiz/Sports code was modified.

Expedition preparation now validates all selected stops against each other and home in both directions using the actual movement budget. This fixes a reproduced 33.94 m connecting leg that the prior origin-only checks admitted but the 24 m movement planner could not route. Preparation yields every eight simulated steps, admits no arrival, and leaves runtime work limits unchanged. All twenty tested seeds produced three mutually reachable stops; average measured CPU was 6.5 ms across yielded work, with a longest timer gap of 7.1 ms in that local sample.

Verification: 2,494 tests pass; production build and targeted ESLint pass. Mobile-sized production Chrome verified four owned creatures, dispatch of a non-party creature, continued physical movement after selecting another companion, two concurrent journeys, and reload retaining all four cards and the travel journal without page errors. The original excursion regression also completed all three actual destinations, return, explicit recall, another reload, and retained Kai-ordered records without superseding the original proof. Test-only generated cards were injected into a fresh browser context for the roster test; no user state or wallet was used. MCP capability inspection returned successfully with no delegated scopes; it establishes tool availability, not write authorization.
