# Wildz v120 AI operating doctrine

Wildz is aligned only to the public npm releases `@receiz/ai-skills@120.0.0`, `@receiz/sdk@120.0.0`, and `@receiz/mcp-server@120.0.0`, with published integrity values pinned in `pnpm-lock.yaml`. All procedures bind to registry digest `0728651789b26e1d10c1991ec1c06c1ea4a576f0c6520537b250b171f8857073`, 30-operation matrix digest `1c779ee5ade4b877ae9c6922ab02ba96fffffeb7580f1cf105a59fbb4424f351`, artifact laws `ARTIFACT-001` through `ARTIFACT-030`, and the v120 profile/economy showcase authority rules.

V120 is one coordinated release identity across SDK, MCP, AI manifests, ruleset, registry, operation matrix, compatible package range, and packed runtime. Durable proof memory is first admission only, then append forever. V120 causal orchestration keeps the enclosing artifact strongest, paints known truth before discovery, selects causal heads by Kai, and commits exact appends through Merkle roots with Fibonacci sparse ancestry.

Sealed proof-object truth and verified local history outrank SDK, MCP, AI, server, database, session, and UI projections. Receiz.com reference behavior comes before developer rails. Never mutate witnessed history; append authenticated truth and rebuild projections. A queued proposal is not a global commitment.

Complete artifacts must cross `artifacts.verifyAndOpen` before any domain parser sees `verifiedPayload.bytes`. Current creation uses native Record → Seal, exact `artifacts.download`, an independent byte digest, and exact-file reopening. Verified legacy artifacts are read-compatible only. Bearer ownership passes only `opened.sealedArtifact` into `ownership.claimBearerAsset`, then downloads and reopens the returned native artifact.

Profile showcases use literal `profile-showcase:<owner>` identity, carry successor history in sealed bytes, and create no new signer, issuer, or head authority. Profile and economy genesis/append/merge planning is zero-write and requires the exact v120 registry/matrix evidence; economy merges additionally require verified sibling heads.

Living subjects add a proof-brain retrieval layer, factual event-cited memory, Twin profile/message/portable-mind operations, mandates, deterministic world transactions and jobs, and bearer instruments. The proof brain is an index, not authority. Model output must pass command admission before it affects the world. A failed decision writes zero including Kai; multi-subject effects are atomic; mandates are reverified at execution; unknown namespaces remain byte-exact; and transfer preserves subject identity while revoking former-owner authority.

For Wildz creature dialogue, ground “who am I?” in the exact proof-object brain and “where am I now?” in the current Kai `uPulse`, coordinate, Ark, world presence, causal head, condition, and relationship. Reason freshly from the owner's actual message. Do not use stock dialogue, expose machine values, or substitute deterministic phrases for real Twin intelligence. An unavailable intelligence appends no conversation.

MCP and AI outputs are operational guidance, never proof authority. Any admitted remote operation requires explicit user confirmation, authenticated authority, and independent verification. Release completion requires law coverage, mutation tests, compatibility, MCP conformance, independent verifier evidence, typecheck, and the release lock.

- `wildz-builder-skill`: preserve artifact custody, continuity, history, and deterministic projections.
- `wildz-market-operator-skill`: use native v120 bearer ownership and fail closed on unavailable admission.
- `wildz-release-skill`: enforce exact package/digest parity and all artifact evidence fields.
