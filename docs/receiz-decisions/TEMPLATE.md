# Receiz capability-gap decision: <outcome>

Status: proposed — not approved for implementation

## Exact release identity

- `@receiz/sdk`:
- `@receiz/mcp-server`:
- `@receiz/ai-skills`:
- ruleset / registry / operation-matrix digests:

## Requested outcome

Describe the user-visible result without proposing infrastructure.

## SDK inventory performed first

List every relevant installed SDK primitive, type, runtime/conformance path, and example inspected. Explain with evidence why none can compose into the requested result.

## MCP inventory performed second

List every relevant installed MCP tool and schema inspected. State the operator/audit/conformance boundary and why no available tool covers it. MCP must not be proposed as runtime or proof authority.

## AI-skill doctrine performed third

List every applicable installed AI skill and directly required reference read completely. Extract the authority, failure, portability, latency, and admission laws that constrain the decision.

## Existing exact proof path

Trace artifact, identity/custody, live head, history, append, export/import, and transfer behavior. Name what must remain byte-exact and unchanged.

## Proven capability gap

State the missing primitive precisely. Documentation not yet found, temporary service failure, unfamiliarity, or implementation urgency is not a gap.

## Minimal proposed addition

Explain why the addition is the smallest possible boundary, creates no alternate authority, and cannot be replaced by a native composition.

## Cost and removal analysis

- Hot path and latency:
- Runtime dependencies and payload:
- Environment/configuration:
- Privacy/security/operational liability:
- Offline/portability/interoperability:
- Failure and zero-write behavior:
- Removal condition when Receiz gains the primitive:

## Executable evidence

List success, rejection, replay/idempotency, stale/conflict, authority, zero-write failure, architecture-lock, official checker, MCP conformance, and live qualification tests.

## Approval

- Reviewer:
- Exact approved scope:
- Date:

No implementation may begin while status remains `proposed`.
