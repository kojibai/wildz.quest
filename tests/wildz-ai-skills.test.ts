import assert from "node:assert/strict"; import { readFileSync } from "node:fs"; import { test } from "node:test";
test("Wildz AI skills state v103 proof authority and confirmation law", () => { for (const name of ["wildz-builder-skill","wildz-market-operator-skill","wildz-release-skill"]) { const source = readFileSync(`ai-skills/${name}/SKILL.md`,"utf8"); assert.match(source,/Receiz proof authority/i); assert.match(source,/major 103|\^103\.0\.0/i); assert.match(source,/confirmation/i); assert.doesNotMatch(source,/major 102|Bearer\s+[A-Za-z0-9_-]{12}/); } });
